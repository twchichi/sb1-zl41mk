export interface Contact {
  id: string;
  name: string;
  company: string;
  title: string;
  address: string;
  phone: string;
  mobile: string;
  email: string;
}

export interface Interaction {
  date: string;
  description: string;
}